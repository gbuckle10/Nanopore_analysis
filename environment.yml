# environment.yml
# This file defines the software environment for the nanopore analysis pipeline.
# It lists the dependencies for the pipeline to work properly.

name: nanopore_analysis

channels:
  # Conda will search these channels in this order when it tries to find a program
  - conda-forge
  - bioconda
  - nanoporetech
  - defaults
dependencies:
  - python=3.9
  - unzip
  - curl
  - pyarrow=20.0 # Can't remember why it's pinned to 20, I think it's a dependency of another dependency
  - go-yq
  - bedtools
  - pybedtools
  - requests
  - pandas
  - scipy # Required by submodules
  - matplotlib # Required by submodules
  - conda-forge::gxx
  - pysam
  - pydantic
  - tqdm
  - pip
  - pip:
    - pyyaml
    - boto3
    #- pandas
    #- scipy
    #- matplotlib
    - colorlog
  # --- Conda Packages ---
  - minimap2
  - samtools
  - pod5
  - typing-extensions
  - modkit